#Conditions of Use
XPATH:Sx("//a[contains(@href,'/gp/help/customer/display.html/ref=ap_signin_notification_condition_of_use?ie=UTF8&nodeId=508088"')]

#Email field
driver.find_element(By.ID, "ap_email_login")

#Continue button
driver.find_element(By.ID, ="continue-announce")

#Conditions of use link
driver.find_element(By.ID, = "Conditions of Use")

#Privacy Notice link
Link Text: Conditions of Use
driver.find_element(By.LINK_TEXT, "Conditions of Use").send_keys("")

#Need help link
Link Text: Privacy Notice
driver.find_element(By.LINK_TEXT, "Privacy Notice").send_keys("")

#Forgot your password link
Link Text: Password Reset
driver.find_element(By.LINK_TEXT, "Privacy Notice").send_keys("")

#Other issues with Sign-In link
id = "ap-other-signin-issues-link"
driver.find_element(By.ID, = "ap-other-signin-issues-link")

#Create your Amazon account button
Link Text: Create your Amazon account
driver.find_element(By.LINK_TEXT, "Create your Amazon account").send_keys("")

Python

from time import sleep
from selenium import webdriver
from selenium.webdriver.common.by import By

driver = webdriver.Chrome()
driver.maximize_window()

driver.get("https://www.target.com/")
sleep(2)

  # Step 1: Open Target homepage
    driver.get("https://www.target.com/")
    time.sleep(3)

    # Step 2: Click Account button
    account_btn = driver.find_element(By.XPATH, class="//button[contains(@aria-label,'Account')]")
    account_btn.click()
    time.sleep(2)

    # Step 3: Click Sign In from side navigation
    signin_btn = driver.find_element(By.XPATH, class="//a[contains(text(),'Sign in')]")
    signin_btn.click()
    time.sleep(3)

    # Step 4: Verify Sign-In page opened
    signin_header = driver.find_element(
        By.XPATH, class="//h1[contains(text(),'Sign in') or contains(text(),'create account')]"
    )

    # Verify Sign-In button is present
    signin_button = driver.find_element(
        By.XPATH, class="//button[contains(text(),'Sign in')]"
    )

    print("Sign-In page elements found successfully.")

Lastly:
    time.sleep(3)
    driver.quit()
